version: "2"
services:
  mongo:
    image: mongo
    restart: always
    ports:
    - 27017:27017
    volumes:
    - ./data/db:/data/db
  # backend:
  #   restart: always
  #   build: ./back
  #   environment:
  #     APP_NAME: chordlib:server
  #     DEBUG: chordlib:*
  #     PORT: 4000
  #     MONGO_URI: mongodb://mongo/chords
  #     FOLDERS.WIKI: ./files/public/pages/wiki
  #     FOLDERS.ARTIST_PAGES: ./files/artists
  #   ports:
  #   - "4000:4000"
  #   links:
  #   - mongo
  #   volumes:
  #   - ./back/src:/app/src
  #   - ./back/public:/app/public
  frontend:
    restart: always
    build: ./front
    ports:
    - 8080:8080
    # links:
    # - backend
    volumes:
    - ./front/src:/app/src
    - ./front/public:/app/public
  mongo-express:
    image: mongo-express
    restart: always
    ports:
    - 8081:8081
    links:
    - mongo
