/*
import {
    model,
    Schema,
} from 'mongoose';

const Client = new Schema({
    name: {
        type: String,
        unique: true,
        required: true
    },
    clientId: {
        type: String,
        unique: true,
        required: true
    },
    clientSecret: {
        type: String,
        required: true
    },
});

const ClientModel = model('Client', Client);

export default ClientModel;
*/
