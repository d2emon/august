/*
import {
    model,
    Schema,
} from 'mongoose';

const RefreshToken = new Schema({
    userId: {
        type: String,
        required: true
    },
    clientId: {
        type: String,
        required: true
    },
    token: {
        type: String,
        unique: true,
        required: true
    },
    created: {
        type: Date,
        default: Date.now
    },
});

const RefreshTokenModel = model('RefreshToken', RefreshToken);

export default RefreshTokenModel;
*/
