/*
import {
    model,
    Schema,
} from 'mongoose';

const AccessToken = new Schema({
    userId: {
        type: String,
        required: true
    },
    clientId: {
        type: String,
        required: true
    },
    token: {
        type: String,
        unique: true,
        required: true
    },
    created: {
        type: Date,
        default: Date.now
    },
});

const AccessTokenModel = model('AccessToken', AccessToken);

export default AccessTokenModel;
*/
